<div class="form-group">
    <label>{{localize 'ckl-roll-bonuses.weaponFocus'}}</label>
    <div class="form-fields">
        <select name="current" id="weapon-focus-selector">
            {{#select current}}
            {{#each choices}}
            <option value="{{this}}">{{this}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
</div>