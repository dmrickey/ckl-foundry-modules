<div class="form-group">
    <label>{{localize 'ckl-roll-bonuses.spells.spellFocusSchool'}}</label>
    <div class="form-fields">
        <select name="school" id="spell-focus-selector">
            {{#select currentSchool}}
            {{#each spellSchools as |name key|}}
            <option value="{{key}}">{{name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
</div>